export const InitializationErrors = {
	REQUIRED_ENV_VARS_MISSING:
		'Required environment variables are missing. AQUA_STUDIO_DEFAULT_EMAIL and AQUA_STUDIO_DEFAULT_PASSWORD must be set to initialize the platform.',
};
